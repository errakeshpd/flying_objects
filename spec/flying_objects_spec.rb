require "spec_helper"

describe FlyingObjects do
  it "has a version number" do
    expect(FlyingObjects::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
